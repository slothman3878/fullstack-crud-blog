To run or build the client, we first need to generate the types based on the graphql schema.
To do this, simply
```
  yarn generate
```
If ever you change things and need to "regenerate" the generated code,
```
  yarn regenerate
```